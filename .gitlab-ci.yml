pages:
  stage: deploy
  script:
    - apt-get update && apt-get install -y rsync
    - rm -rf public
    - mkdir public
    - rsync -av --exclude=public . public
  artifacts:
    paths:
      - public
    expire_in: 1 week   # optional, keeps artifacts for a week
  only:
    - main